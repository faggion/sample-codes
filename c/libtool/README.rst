
build
-----------------

.. sourcecode:: shell
  
  % libtool --mode=link gcc -o libone.la -c one.c -rpath `pwd`

clean
-----------------

.. sourcecode:: shell
  
  % libtool --mode=clean rm libone.la

