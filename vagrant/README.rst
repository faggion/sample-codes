

- ssh port forwarding
- which vagrant @ macbook air

.. code-block:: none
  
  gem install vagrant
  vagrant box add debian6 http://ergonlogic.com/files/boxes/debian-current.box
  vagrant init
  e Vagrantfile
  sudo vagrant up
  sudo vagrant ssh
